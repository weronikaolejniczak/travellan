import { Metrics, Fonts, Colors } from 'App/Theme'

const styles = {
    headerText: {
        ...Fonts.h2,
        marginBottom: Metrics.tiny,
        textAlign: 'center',
    },
    text: {
        ...Fonts.normal,
        marginBottom: Metrics.tiny,
        textAlign: 'center',
    },
}

export default styles