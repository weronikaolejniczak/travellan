import { Metrics, Fonts, Colors } from 'App/Theme'

const styles = {
    text: {
        ...Fonts.normal,
        marginBottom: Metrics.tiny,
        textAlign: 'center',
    },
}

export default styles