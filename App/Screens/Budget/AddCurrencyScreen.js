import React from 'react';
import {ScrollView, Text} from 'react-native';

const AddNote = (props) => {
  return (
    <ScrollView>
      <Text>Hey</Text>
    </ScrollView>
  );
};

export default AddNote;
