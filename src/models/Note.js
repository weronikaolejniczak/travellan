class Note {
  constructor(id, date, category, title, description) {
    this.id = id;
    this.date = date;
    this.category = category;
    this.title = title;
    this.description = description;
  }
}

export default Note;
