import {Colors, Layout} from 'constants';

export default {
  container: {
    ...Layout.fill,
    backgroundColor: Colors.background,
    paddingTop: '12%',
  },
  text: {
    color: Colors.text,
  },
};
