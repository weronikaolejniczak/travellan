class Note {
  constructor(id, category, title, description) {
    this.id = id;
    this.category = category;
    this.title = title;
    this.description = description;
  }
}

export default Note;
