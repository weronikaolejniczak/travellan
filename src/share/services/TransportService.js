/**
 * get value and type
 *
 * if type === media, scheme === file, includes '.pdf'
 *  parse with pdf.js
 *  if success
 *      return transport model
 *  else
 *      return error
 * else
 *  return error
 */
