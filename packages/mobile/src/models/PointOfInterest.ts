class PointOfInterest {
  constructor(key, id, lat, lon, title) {
    this.key = key;
    this.id = id;
    this.lat = lat;
    this.lon = lon;
    this.title = title;
  }
}

export default PointOfInterest;
